// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/** Encode framerate parameters.
 *
 * Sets the encode framerate used by the rate controller.  This should be
 * provided in all modes using a bitrate target (variable framerate is not
 * supported).
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAEncMiscParameterFrameRate extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAEncMiscParameterFrameRate() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAEncMiscParameterFrameRate(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAEncMiscParameterFrameRate(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAEncMiscParameterFrameRate position(long position) {
        return (VAEncMiscParameterFrameRate)super.position(position);
    }
    @Override public VAEncMiscParameterFrameRate getPointer(long i) {
        return new VAEncMiscParameterFrameRate((Pointer)this).offsetAddress(i);
    }

    /** Encode framerate.
     *
     * The framerate is specified as a number of frames per second, as a
     * fraction.  The denominator of the fraction is given in the top half
     * (the high two bytes) of the framerate field, and the numerator is
     * given in the bottom half (the low two bytes).
     *
     * That is:
     * denominator = framerate >> 16 & 0xffff;
     * numerator   = framerate & 0xffff;
     * fps         = numerator / denominator;
     *
     * For example, if framerate is set to (100 << 16 | 750), this is
     * 750 / 100, hence 7.5fps.
     *
     * If the denominator is zero (the high two bytes are both zero) then
     * it takes the value one instead, so the framerate is just the integer
     * in the low 2 bytes.
     */
    public native @Cast("uint32_t") int framerate(); public native VAEncMiscParameterFrameRate framerate(int setter);
            /** The temporal layer that these framerate parameters apply to. */
            @Name("framerate_flags.bits.temporal_id") public native @Cast("uint32_t") @NoOffset int framerate_flags_bits_temporal_id(); public native VAEncMiscParameterFrameRate framerate_flags_bits_temporal_id(int setter);
            /** Reserved for future use, must be zero. */
            @Name("framerate_flags.bits.reserved") public native @Cast("uint32_t") @NoOffset int framerate_flags_bits_reserved(); public native VAEncMiscParameterFrameRate framerate_flags_bits_reserved(int setter);
        @Name("framerate_flags.value") public native @Cast("uint32_t") int framerate_flags_value(); public native VAEncMiscParameterFrameRate framerate_flags_value(int setter);

    /** \brief Reserved bytes for future use, must be zero */
    public native @Cast("uint32_t") int va_reserved(int i); public native VAEncMiscParameterFrameRate va_reserved(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer va_reserved();
}
