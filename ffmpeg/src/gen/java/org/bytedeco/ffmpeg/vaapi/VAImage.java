// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAImage extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAImage() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAImage(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAImage(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAImage position(long position) {
        return (VAImage)super.position(position);
    }
    @Override public VAImage getPointer(long i) {
        return new VAImage((Pointer)this).offsetAddress(i);
    }

    public native @Cast("VAImageID") int image_id(); public native VAImage image_id(int setter); /* uniquely identify this image */
    public native @ByRef VAImageFormat format(); public native VAImage format(VAImageFormat setter);
    public native @Cast("VABufferID") int buf(); public native VAImage buf(int setter);    /* image data buffer */
    /*
     * Image data will be stored in a buffer of type VAImageBufferType to facilitate
     * data store on the server side for optimal performance. The buffer will be
     * created by the CreateImage function, and proper storage allocated based on the image
     * size and format. This buffer is managed by the library implementation, and
     * accessed by the client through the buffer Map/Unmap functions.
     */
    public native @Cast("uint16_t") short width(); public native VAImage width(short setter);
    public native @Cast("uint16_t") short height(); public native VAImage height(short setter);
    public native @Cast("uint32_t") int data_size(); public native VAImage data_size(int setter);
    public native @Cast("uint32_t") int num_planes(); public native VAImage num_planes(int setter); /* can not be greater than 3 */
    /*
     * An array indicating the scanline pitch in bytes for each plane.
     * Each plane may have a different pitch. Maximum 3 planes for planar formats
     */
    public native @Cast("uint32_t") int pitches(int i); public native VAImage pitches(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer pitches();
    /*
     * An array indicating the byte offset from the beginning of the image data
     * to the start of each plane.
     */
    public native @Cast("uint32_t") int offsets(int i); public native VAImage offsets(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer offsets();

    /* The following fields are only needed for paletted formats */
    public native int num_palette_entries(); public native VAImage num_palette_entries(int setter);   /* set to zero for non-palette images */
    /*
     * Each component is one byte and entry_bytes indicates the number of components in
     * each entry (eg. 3 for YUV palette entries). set to zero for non-palette images
     */
    public native int entry_bytes(); public native VAImage entry_bytes(int setter);
    /*
     * An array of ascii characters describing the order of the components within the bytes.
     * Only entry_bytes characters of the string are used.
     */
    public native byte component_order(int i); public native VAImage component_order(int i, byte setter);
    @MemberGetter public native BytePointer component_order();

    /** \brief Reserved bytes for future use, must be zero */
    public native @Cast("uint32_t") int va_reserved(int i); public native VAImage va_reserved(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer va_reserved();
}
