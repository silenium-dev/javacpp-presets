// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/**
 * \brief External buffer descriptor for a DRM PRIME surface.
 *
 * For export, call vaExportSurfaceHandle() with mem_type set to
 * VA_SURFACE_ATTRIB_MEM_TYPE_DRM_PRIME_2 and pass a pointer to an
 * instance of this structure to fill.
 * If VA_EXPORT_SURFACE_SEPARATE_LAYERS is specified on export, each
 * layer will contain exactly one plane.  For example, an NV12
 * surface will be exported as two layers, one of DRM_FORMAT_R8 and
 * one of DRM_FORMAT_GR88.
 * If VA_EXPORT_SURFACE_COMPOSED_LAYERS is specified on export,
 * there will be exactly one layer.
 *
 * For import, call vaCreateSurfaces() with the MemoryType attribute
 * set to VA_SURFACE_ATTRIB_MEM_TYPE_DRM_PRIME_2 and the
 * ExternalBufferDescriptor attribute set to point to an array of
 * num_surfaces instances of this structure.
 * The number of planes which need to be provided for a given layer
 * is dependent on both the format and the format modifier used for
 * the objects containing it.  For example, the format DRM_FORMAT_RGBA
 * normally requires one plane, but with the format modifier
 * I915_FORMAT_MOD_Y_TILED_CCS it requires two planes - the first
 * being the main data plane and the second containing the color
 * control surface.
 * Note that a given driver may only support a subset of possible
 * representations of a particular format.  For example, it may only
 * support NV12 surfaces when they are contained within a single DRM
 * object, and therefore fail to create such surfaces if the two
 * planes are in different DRM objects.
 * Note that backend driver will retrieve the resource represent by fd,
 * and a valid surface ID is generated. Backend driver will not close
 * the file descriptor. Application should handle the release of the fd.
 * releasing the fd will not impact the existence of the surface.
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VADRMPRIMESurfaceDescriptor extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VADRMPRIMESurfaceDescriptor() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VADRMPRIMESurfaceDescriptor(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VADRMPRIMESurfaceDescriptor(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VADRMPRIMESurfaceDescriptor position(long position) {
        return (VADRMPRIMESurfaceDescriptor)super.position(position);
    }
    @Override public VADRMPRIMESurfaceDescriptor getPointer(long i) {
        return new VADRMPRIMESurfaceDescriptor((Pointer)this).offsetAddress(i);
    }

    /** Pixel format fourcc of the whole surface (VA_FOURCC_*). */
    public native @Cast("uint32_t") int fourcc(); public native VADRMPRIMESurfaceDescriptor fourcc(int setter);
    /** Width of the surface in pixels. */
    public native @Cast("uint32_t") int width(); public native VADRMPRIMESurfaceDescriptor width(int setter);
    /** Height of the surface in pixels. */
    public native @Cast("uint32_t") int height(); public native VADRMPRIMESurfaceDescriptor height(int setter);
    /** Number of distinct DRM objects making up the surface. */
    public native @Cast("uint32_t") int num_objects(); public native VADRMPRIMESurfaceDescriptor num_objects(int setter);
    /** Description of each object. */
        /** DRM PRIME file descriptor for this object. */
        @Name({"objects", ".fd"}) public native int objects_fd(int i); public native VADRMPRIMESurfaceDescriptor objects_fd(int i, int setter);
        /** Total size of this object (may include regions which are
         *  not part of the surface). */
        @Name({"objects", ".size"}) public native @Cast("uint32_t") int objects_size(int i); public native VADRMPRIMESurfaceDescriptor objects_size(int i, int setter);
        /** Format modifier applied to this object. */
        @Name({"objects", ".drm_format_modifier"}) public native @Cast("uint64_t") long objects_drm_format_modifier(int i); public native VADRMPRIMESurfaceDescriptor objects_drm_format_modifier(int i, long setter);
    /** Number of layers making up the surface. */
    public native @Cast("uint32_t") int num_layers(); public native VADRMPRIMESurfaceDescriptor num_layers(int setter);
    /** Description of each layer in the surface. */
        /** DRM format fourcc of this layer (DRM_FOURCC_*). */
        @Name({"layers", ".drm_format"}) public native @Cast("uint32_t") int layers_drm_format(int i); public native VADRMPRIMESurfaceDescriptor layers_drm_format(int i, int setter);
        /** Number of planes in this layer. */
        @Name({"layers", ".num_planes"}) public native @Cast("uint32_t") int layers_num_planes(int i); public native VADRMPRIMESurfaceDescriptor layers_num_planes(int i, int setter);
        /** Index in the objects array of the object containing each
         *  plane. */
        @Name({"layers", ".object_index"}) @MemberGetter public native @Cast("uint32_t*") IntPointer layers_object_index(int i);
        /** Offset within the object of each plane. */
        @Name({"layers", ".offset"}) @MemberGetter public native @Cast("uint32_t*") IntPointer layers_offset(int i);
        /** Pitch of each plane. */
        @Name({"layers", ".pitch"}) @MemberGetter public native @Cast("uint32_t*") IntPointer layers_pitch(int i);
}
