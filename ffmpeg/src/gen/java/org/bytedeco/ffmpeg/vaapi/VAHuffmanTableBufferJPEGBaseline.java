// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/**********************************
 * JPEG common  data structures
 **********************************/
/**
 * \brief Huffman table for JPEG decoding.
 *
 * This structure holds the complete Huffman tables. This is an
 * aggregation of all Huffman table (DHT) segments maintained by the
 * application. i.e. up to 2 Huffman tables are stored in there for
 * baseline profile.
 *
 * The #load_huffman_table array can be used as a hint to notify the
 * VA driver implementation about which table(s) actually changed
 * since the last submission of this buffer.
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAHuffmanTableBufferJPEGBaseline extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAHuffmanTableBufferJPEGBaseline() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAHuffmanTableBufferJPEGBaseline(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAHuffmanTableBufferJPEGBaseline(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAHuffmanTableBufferJPEGBaseline position(long position) {
        return (VAHuffmanTableBufferJPEGBaseline)super.position(position);
    }
    @Override public VAHuffmanTableBufferJPEGBaseline getPointer(long i) {
        return new VAHuffmanTableBufferJPEGBaseline((Pointer)this).offsetAddress(i);
    }

    /** \brief Specifies which #huffman_table is valid. */
    public native @Cast("uint8_t") byte load_huffman_table(int i); public native VAHuffmanTableBufferJPEGBaseline load_huffman_table(int i, byte setter);
    @MemberGetter public native @Cast("uint8_t*") BytePointer load_huffman_table();
    /** \brief Huffman tables indexed by table identifier (Th). */
        /** \name DC table (up to 12 categories) */
        /**\{*/
        /** \brief Number of Huffman codes of length i + 1 (Li). */
        @Name({"huffman_table", ".num_dc_codes"}) @MemberGetter public native @Cast("uint8_t*") BytePointer huffman_table_num_dc_codes(int i);
        /** \brief Value associated with each Huffman code (Vij). */
        @Name({"huffman_table", ".dc_values"}) @MemberGetter public native @Cast("uint8_t*") BytePointer huffman_table_dc_values(int i);
        /**\}*/
        /** \name AC table (2 special codes + up to 16 * 10 codes) */
        /**\{*/
        /** \brief Number of Huffman codes of length i + 1 (Li). */
        @Name({"huffman_table", ".num_ac_codes"}) @MemberGetter public native @Cast("uint8_t*") BytePointer huffman_table_num_ac_codes(int i);
        /** \brief Value associated with each Huffman code (Vij). */
        @Name({"huffman_table", ".ac_values"}) @MemberGetter public native @Cast("uint8_t*") BytePointer huffman_table_ac_values(int i);
        /** \brief Padding to 4-byte boundaries. Must be set to zero. */
        @Name({"huffman_table", ".pad"}) @MemberGetter public native @Cast("uint8_t*") BytePointer huffman_table_pad(int i);
        /**\}*/

    /** \brief Reserved bytes for future use, must be zero */
    public native @Cast("uint32_t") int va_reserved(int i); public native VAHuffmanTableBufferJPEGBaseline va_reserved(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer va_reserved();
}
