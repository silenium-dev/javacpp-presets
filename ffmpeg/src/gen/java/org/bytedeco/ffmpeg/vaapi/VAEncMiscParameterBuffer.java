// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/**
 *  For application, e.g. set a new bitrate
 *    VABufferID buf_id;
 *    VAEncMiscParameterBuffer *misc_param;
 *    VAEncMiscParameterRateControl *misc_rate_ctrl;
 *
 *    vaCreateBuffer(dpy, context, VAEncMiscParameterBufferType,
 *              sizeof(VAEncMiscParameterBuffer) + sizeof(VAEncMiscParameterRateControl),
 *              1, NULL, &buf_id);
 *
 *    vaMapBuffer(dpy,buf_id,(void **)&misc_param);
 *    misc_param->type = VAEncMiscParameterTypeRateControl;
 *    misc_rate_ctrl= (VAEncMiscParameterRateControl *)misc_param->data;
 *    misc_rate_ctrl->bits_per_second = 6400000;
 *    vaUnmapBuffer(dpy, buf_id);
 *    vaRenderPicture(dpy, context, &buf_id, 1);
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAEncMiscParameterBuffer extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAEncMiscParameterBuffer() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAEncMiscParameterBuffer(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAEncMiscParameterBuffer(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAEncMiscParameterBuffer position(long position) {
        return (VAEncMiscParameterBuffer)super.position(position);
    }
    @Override public VAEncMiscParameterBuffer getPointer(long i) {
        return new VAEncMiscParameterBuffer((Pointer)this).offsetAddress(i);
    }

    public native @Cast("uint32_t") int data(int i); public native VAEncMiscParameterBuffer data(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer data();
}
