// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/** \brief Encryption parameters buffer for VAEncryptionParameterBufferType */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAEncryptionParameters extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAEncryptionParameters() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAEncryptionParameters(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAEncryptionParameters(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAEncryptionParameters position(long position) {
        return (VAEncryptionParameters)super.position(position);
    }
    @Override public VAEncryptionParameters getPointer(long i) {
        return new VAEncryptionParameters((Pointer)this).offsetAddress(i);
    }

    /** \brief Encryption type, refer to \c VA_ENCRYPTION_TYPE_FULLSAMPLE_CTR,
     * \c VA_ENCRYPTION_TYPE_FULLSAMPLE_CBC, \c VA_ENCRYPTION_TYPE_SUBSAMPLE_CTR,
     * or \c VA_ENCRYPTION_TYPE_SUBSAMPLE_CBC */
    public native @Cast("uint32_t") int encryption_type(); public native VAEncryptionParameters encryption_type(int setter);
    /** \brief The number of sengments */
    public native @Cast("uint32_t") int num_segments(); public native VAEncryptionParameters num_segments(int setter);
    /** \brief Pointer of segments */
    public native VAEncryptionSegmentInfo segment_info(); public native VAEncryptionParameters segment_info(VAEncryptionSegmentInfo setter);
    /** \brief The status report index reserved for CENC fullsample workload.
     * The related structures and definitions are vendor specific.
    */
    public native @Cast("uint32_t") int status_report_index(); public native VAEncryptionParameters status_report_index(int setter);
    /** \brief CENC counter length */
    public native @Cast("uint32_t") int size_of_length(); public native VAEncryptionParameters size_of_length(int setter);
    /** \brief Wrapped decrypt blob (Snd)kb, valid size is specified by
     * \c key_blob_size */
    public native @Cast("uint8_t") byte wrapped_decrypt_blob(int i); public native VAEncryptionParameters wrapped_decrypt_blob(int i, byte setter);
    @MemberGetter public native @Cast("uint8_t*") BytePointer wrapped_decrypt_blob();
    /** \brief Wrapped Key blob info (Sne)kb, valid size is specified by
     * \c key_blob_size */
    public native @Cast("uint8_t") byte wrapped_encrypt_blob(int i); public native VAEncryptionParameters wrapped_encrypt_blob(int i, byte setter);
    @MemberGetter public native @Cast("uint8_t*") BytePointer wrapped_encrypt_blob();
    /** \brief key blob size
     * It could be \c VA_PC_BLOCK_SIZE_128, \c VA_PC_BLOCK_SIZE_192, or
     * \c VA_PC_BLOCK_SIZE_256
     */
    public native @Cast("uint32_t") int key_blob_size(); public native VAEncryptionParameters key_blob_size(int setter);
    /** \brief Indicates the number of 16-byte BLOCKS that are encrypted in any
     *  given encrypted region of segments.
     *  If this value is zero:
     *    1. All bytes in encrypted region of segments are encrypted, i.e. the
     *       CENC or CBC1 scheme is being used
     *    2. blocks_stripe_clear must also be zero.
     *  If this value is non-zero, blocks_stripe_clear must also be non-zero.
     */
    public native @Cast("uint32_t") int blocks_stripe_encrypted(); public native VAEncryptionParameters blocks_stripe_encrypted(int setter);
    /** \brief Indicates the number of 16-byte BLOCKS that are clear in any given
     *  encrypted region of segments, as defined by the CENS and CBCS schemes in
     *  the common encryption spec.
     *  If this value is zero, all bytes in encrypted region of segments are
     *  encrypted, i.e. the CENC or CBC1 scheme is being used.
     */
    public native @Cast("uint32_t") int blocks_stripe_clear(); public native VAEncryptionParameters blocks_stripe_clear(int setter);
    /** \brief Reserved bytes for future use, must be zero */
    public native @Cast("uint32_t") int va_reserved(int i); public native VAEncryptionParameters va_reserved(int i, int setter);
    @MemberGetter public native @Cast("uint32_t*") IntPointer va_reserved();
}
