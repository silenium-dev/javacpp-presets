// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;

/*
 * \brief Dirty rectangle data structure for encoding.
 *
 * The encoding dirty rect can be set through VAEncMiscParameterBufferDirtyRect, if the
 * implementation supports dirty rect input. The rect set through this structure is applicable
 * only to the current frame or field, so must be sent every frame or field to be applied.
 * The number of supported rects can be queried through the VAConfigAttribEncDirtyRect.  The
 * encoder will use the rect information to know those rectangle areas have changed while the
 * areas not covered by dirty rect rectangles are assumed to have not changed compared to the
 * previous picture.  The encoder may do some internal optimizations.
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VAEncMiscParameterBufferDirtyRect extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VAEncMiscParameterBufferDirtyRect() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VAEncMiscParameterBufferDirtyRect(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VAEncMiscParameterBufferDirtyRect(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VAEncMiscParameterBufferDirtyRect position(long position) {
        return (VAEncMiscParameterBufferDirtyRect)super.position(position);
    }
    @Override public VAEncMiscParameterBufferDirtyRect getPointer(long i) {
        return new VAEncMiscParameterBufferDirtyRect((Pointer)this).offsetAddress(i);
    }

    /** \brief Number of Rectangle being sent.*/
    public native @Cast("uint32_t") int num_roi_rectangle(); public native VAEncMiscParameterBufferDirtyRect num_roi_rectangle(int setter);

    /** \brief Pointer to a VARectangle array with num_roi_rectangle elements.*/
    public native VARectangle roi_rectangle(); public native VAEncMiscParameterBufferDirtyRect roi_rectangle(VARectangle setter);
}
