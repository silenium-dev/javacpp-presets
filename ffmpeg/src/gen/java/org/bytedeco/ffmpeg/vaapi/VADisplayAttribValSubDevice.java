// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.vaapi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.vaapi.*;


/**\brief sub device info
 * Sub-device is the concept basing on the "device" behind "vaDisplay".
 * If a device could be divided to several sub devices, the task of
 * decode/encode/vpp could be assigned on one sub-device. So, application
 * could choose the sub device before any other operations. After that,
 * all of the task execution/resource allocation will be dispatched to
 * the sub device. If application does not choose the sub device, driver
 * will assign one as default.
 *
 * If the value == VA_ATTRIB_NOT_SUPPORTED, it mean that the attribute
 * is unsupport or UNKNOWN.
 */

@Properties(inherit = org.bytedeco.ffmpeg.presets.vaapi.class)
public class VADisplayAttribValSubDevice extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public VADisplayAttribValSubDevice() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public VADisplayAttribValSubDevice(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public VADisplayAttribValSubDevice(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public VADisplayAttribValSubDevice position(long position) {
        return (VADisplayAttribValSubDevice)super.position(position);
    }
    @Override public VADisplayAttribValSubDevice getPointer(long i) {
        return new VADisplayAttribValSubDevice((Pointer)this).offsetAddress(i);
    }

        /** \brief current sub device index, read - write */
        @Name("bits.current_sub_device") public native @Cast("uint32_t") @NoOffset int bits_current_sub_device(); public native VADisplayAttribValSubDevice bits_current_sub_device(int setter);
        /** \brief sub devices count, read - only */
        @Name("bits.sub_device_count") public native @Cast("uint32_t") @NoOffset int bits_sub_device_count(); public native VADisplayAttribValSubDevice bits_sub_device_count(int setter);
        /** \brief reserved bits for future, must be zero*/
        @Name("bits.reserved") public native @Cast("uint32_t") @NoOffset int bits_reserved(); public native VADisplayAttribValSubDevice bits_reserved(int setter);
        /** \brief bit mask to indicate which sub_device is available, read only
         * <pre>{@code
         * VADisplayAttribValSubDevice reg;
         * VADisplayAttribute reg_attr;
         * reg_attr.type = VADisplayAttribSubDevice;
         * vaGetDisplayAttributes(dpy, &reg_attr, 1);
         * reg.value = reg_attr.value;
         *
         * for(int i = 0; i < reg.bits.sub_device_count; i ++ ){
         *    if((1<<i) & reg.bits.sub_device_mask){
         *        printf("sub device  %d can be selected", i);
         *    }
         *}
         * }</pre>
         */
        @Name("bits.sub_device_mask") public native @Cast("uint32_t") @NoOffset int bits_sub_device_mask(); public native VADisplayAttribValSubDevice bits_sub_device_mask(int setter);
    public native @Cast("uint32_t") int value(); public native VADisplayAttribValSubDevice value(int setter);
}
