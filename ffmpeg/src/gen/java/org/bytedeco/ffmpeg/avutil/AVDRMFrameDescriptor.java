// Targeted by JavaCPP version 1.5.12-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ffmpeg.avutil;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ffmpeg.global.avutil.*;


/**
 * DRM frame descriptor.
 *
 * This is used as the data pointer for AV_PIX_FMT_DRM_PRIME frames.
 * It is also used by user-allocated frame pools - allocating in
 * AVHWFramesContext.pool must return AVBufferRefs which contain
 * an object of this type.
 *
 * The fields of this structure should be set such it can be
 * imported directly by EGL using the EGL_EXT_image_dma_buf_import
 * and EGL_EXT_image_dma_buf_import_modifiers extensions.
 * (Note that the exact layout of a particular format may vary between
 * platforms - we only specify that the same platform should be able
 * to import it.)
 *
 * The total number of planes must not exceed AV_DRM_MAX_PLANES, and
 * the order of the planes by increasing layer index followed by
 * increasing plane index must be the same as the order which would
 * be used for the data pointers in the equivalent software format.
 */
@Properties(inherit = org.bytedeco.ffmpeg.presets.avutil.class)
public class AVDRMFrameDescriptor extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public AVDRMFrameDescriptor() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public AVDRMFrameDescriptor(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public AVDRMFrameDescriptor(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public AVDRMFrameDescriptor position(long position) {
        return (AVDRMFrameDescriptor)super.position(position);
    }
    @Override public AVDRMFrameDescriptor getPointer(long i) {
        return new AVDRMFrameDescriptor((Pointer)this).offsetAddress(i);
    }

    /**
     * Number of DRM objects making up this frame.
     */
    public native int nb_objects(); public native AVDRMFrameDescriptor nb_objects(int setter);
    /**
     * Array of objects making up the frame.
     */
    public native @ByRef AVDRMObjectDescriptor objects(int i); public native AVDRMFrameDescriptor objects(int i, AVDRMObjectDescriptor setter);
    @MemberGetter public native AVDRMObjectDescriptor objects();
    /**
     * Number of layers in the frame.
     */
    public native int nb_layers(); public native AVDRMFrameDescriptor nb_layers(int setter);
    /**
     * Array of layers in the frame.
     */
    public native @ByRef AVDRMLayerDescriptor layers(int i); public native AVDRMFrameDescriptor layers(int i, AVDRMLayerDescriptor setter);
    @MemberGetter public native AVDRMLayerDescriptor layers();
}
